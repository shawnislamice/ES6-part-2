const numbers = [12, 10, 8, 15, 7];
const doubled = numbers.map((num) => num * 2);
console.log(doubled);

const five_added = numbers.map((n) => n + 5);
console.log(five_added);

const half = numbers.map((n) => n / 2);
console.log(half);

const friends = ["Tom", "John", "Michael", "Oliver"];
const length = friends.map((l) => l.length);
console.log(length)
